apiVersion: v1
kind: Service
metadata:
  name: homarr-svc
  labels:
    {{- include "homarr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: ui
  selector:
    app: homarr-pod
